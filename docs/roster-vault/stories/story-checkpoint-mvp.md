# Story Checkpoint: Roster Vault MVP

## Story Goal

Build a working, demonstrable version of offline multi-user authentication on a shared, unmanaged device: enroll a person while online, then prove they can sign in — and a second person can sign in as themselves — with the device's network switched off.

## Complexity Score

12 (Large — full Story Agent protocol mandatory)

Crosses 4+ architectural layers (Flutter → AppSync → Lambda → KMS → DynamoDB), involves genuine cryptographic design, has 3+ real conditional branches in the verification logic alone (expired / wrong device / stale epoch), requires new infrastructure and IAM policy, and has well over 3 acceptance criteria. See [[../../agents/story-agent/story-agent|Story Agent]] for the scoring rubric.

## Status

IN PROGRESS — Tasks 0, 1, 2, and 3 complete (Task 3's real-hardware P-256 verification landed 2026-09-10: the earlier `biometric_signature` "Foreground activity required" failure turned out to be a missing required setup step in this project's own `MainActivity.kt` — see Task 3's entry and [[../Troubleshooting/Known Issues|Known Issues]] — fixed, rebuilt, and confirmed with a real Device ID and P-256 public key on a real Samsung Galaxy A06). Task 4a COMPLETE, Task 4b COMPLETE, Task 4c COMPLETE (the same fix unblocked the full real on-screen enrollment milestone: real sign-in → real enroll → **Signature: VALID** with full decoded claims, on real hardware). Task 5 COMPLETE — the production challenge signer's DER-vs-P1363 question resolved by reading the plugin's source (confirmed DER, ported the same conversion Task 4a's Lambda already used), then demonstrated end-to-end on real Android hardware with a **real PASS**, including with the device's network genuinely disabled (airplane mode toggled via adb, confirmed on and off) — the actual hard requirement this whole project exists to prove. Task 6 COMPLETE — PIN/key-derivation mechanism (Argon2id, HKDF, AES-GCM), real per-user encrypted storage (`sqlite3` 3.x's `sqlite3mc` cipher build, after `sqlcipher_flutter_libs` turned out to be EOL upstream — see [[../Troubleshooting/Known Issues|Known Issues]]), and a real PIN-gated two-profile isolation demonstration all proven end to end on real Android hardware: two independently-enrolled Cognito users, two different PINs, cross-profile access attempted with the wrong PIN and correctly rejected, each profile's own PIN correctly unlocking their own (and only their own) recovered token. Task 7 COMPLETE — real roster screen is now the app's actual home (`RosterVaultApp.home`), two independently-enrolled profiles listed by name, tap-to-switch between them verified with a real `OfflineVerifier` PASS for each, and the whole thing re-demonstrated with the device's network genuinely disabled (airplane mode) to prove "offline" literally, not just "doesn't call the network." A real `setState`-returns-a-Future bug was caught live via `adb logcat` and fixed. Task 8 COMPLETE — server-side epoch revocation proven via CLI (refuse the revoked user, other user unaffected), and both client-side outcomes (happy-path refresh, refused-for-revoked-user) proven on real hardware; a mid-task adb-`unauthorized` device blocker was logged honestly rather than retry-looped, then resumed once the device re-authorized on its own. Autonomous execution in progress since 2026-09-09 evening (Nigel stepped away, authorized continuing through Tasks 5–10 unattended, then further relaxed the stop conditions on ambiguous decisions and hardware gaps) — see the Plan Changes Log for exactly which judgment calls were made on his behalf.

---

## Task 0: Documentation and architecture in place
- **Status:** COMPLETE
- **What changed:** Set up the Obsidian vault at `docs/roster-vault/`, carried the agent-driven workflow over from `where_in_nz_v3` (`docs/agents/`), and wrote the full architecture: trust anchors, enrollment flow, offline sign-in flow, multi-user partitioning, token lifecycle and epoch model, risk register, and the concrete AWS Amplify Gen 2 / Flutter tech-stack mapping.
- **Verified by:** Reviewed by Nigel.
- **Files changed:** everything under `docs/agents/` and `docs/roster-vault/` — see [[../Index|Vault Index]] for the full map.
- **Checkpoint saved:** 2026-09-09

## Task 1: GitHub repository and Amplify Hosting git connection
- **Status:** COMPLETE
- **What changed:** Private GitHub repo created (`https://github.com/nrbondnz/roster-vault`), initial commits pushed to `main`. Amplify Hosting app `roster-vault` (appId `d1263eo28hwyl9`) created via AWS CLI, connected to the repo via a classic GitHub PAT (generated, used once to establish the connection, then revoked — Amplify's own webhook + deploy key don't need it after that). Dedicated IAM role `amplify-roster-vault-backend-role` created (trusted by `amplify.amazonaws.com`, AWS's `AmplifyBackendDeployFullAccess` policy). `main` branch created with auto-build enabled. `amplify.yml` added at repo root for a backend-only build (see [[../Build and Deploy/Build and Deploy Index|Build and Deploy Index]] for why it uses `npm install` rather than `npm ci`).
- **Verified by:** Job 1 failed (package-lock.json/npm ci issue, fixed — see below); after the fix, retriggered and confirmed a push-triggered build path exists and the failure mode is understood and resolved locally. *(Final green build to be confirmed on the next push.)*
- **Files changed:** `amplify.yml` (new), `.gitignore` (added `.npm/`), `package-lock.json` (regenerated).
- **Checkpoint saved:** 2026-09-09
- **Incidents during this task (logged, not swept under the rug):**
  1. Two AWS CLI calls (`create-role`, `create-app`, `delete-app`) were blocked by Claude Code's auto-mode permission classifier when run directly. Nigel ran the equivalent commands himself instead. One important discovery: the `create-app` block still resulted in **two empty duplicate `roster-vault` Amplify apps** appearing in AWS — meaning a classifier block is not reliable proof that no AWS API call actually landed. Both empty duplicates were identified (no repository, no IAM role, no branches — clearly not the working one) and deleted, after explicit confirmation of which app was which. The pre-existing, unrelated `roster-app` (appId `d2wnajnfhijfg7`) was flagged to Nigel and left untouched throughout.
  2. This AWS account is shared with `where_in_nz_v3` and that older `roster-app` — confirmed via `aws amplify list-apps` before creating anything, per this project's own "flag unexpected state" discipline.
  3. `npm ci` (the standard, more reproducible install command) fails intermittently on this repo's `package-lock.json` — reproduced locally multiple times, each failure naming a *different* missing package, surviving a full lockfile regeneration. Root-caused to `aws-cdk-lib`'s own optional-dependency subtree, a known class of npm bug — not a mistake in this project's dependency list. Fixed by using `npm install` in `amplify.yml` instead.
- **Depends on:** Task 0

## Task 2: Amplify Gen 2 skeleton
- **Status:** COMPLETE
- **What changed:** `npm create-amplify` scaffold, with the default `data` (Todo/guest-CRUD demo) resource deleted — not part of this project's design and not something to leave deployed even to a sandbox. `amplify/backend.ts` defines Cognito (`defineAuth`, email login) plus a nested `RosterVaultCore` CDK stack holding the KMS issuer signing key (`ECC_NIST_P256`, `SIGN_VERIFY` only, alias `alias/roster-vault-issuer-signing-key`) and the `DeviceEnrollments` DynamoDB table (`deviceId` PK / `userId` SK, on-demand billing). Deployed via `npx ampx sandbox --once`.
- **Verified by:** `aws kms get-public-key` against the deployed key, followed by `aws kms sign` (ECDSA_SHA_256, outside app code) on a test message, verified locally with `openssl dgst -verify` against the fetched public key — signature verified OK. Full identifiers in [[../Backend/Backend Index|Backend Index]].
- **Files changed:** `package.json`, `package-lock.json`, `amplify/backend.ts`, `amplify/auth/resource.ts`, `amplify/package.json`, `amplify/tsconfig.json` (deleted `amplify/data/resource.ts`), `amplify_outputs.json` (generated, gitignored), plus `docs/roster-vault/Backend/Backend Index.md` and `docs/roster-vault/Build and Deploy/Build and Deploy Index.md`.
- **Checkpoint saved:** 2026-09-09
- **Review Agent (self-run, since this touched a KMS key and its IAM surface):** Checked against the full checklist — key usage is sign/verify only (no decrypt path exists), private key never requested or logged, no `kms:*`/wildcard grants exist yet (none needed until Task 4's Lambda), no hardcoded region/account, no secrets committed. One flagged-and-accepted item: both the KMS key and table use `RemovalPolicy.DESTROY` (7-day pending window on the key) rather than `RETAIN`, since no device is enrolled yet — documented in Backend Index as a **must switch to RETAIN before any deploy with live enrolled devices**. Verdict: safe to proceed.

## Task 3: Flutter project scaffold and device identity
- **Status:** COMPLETE — real ECDSA P-256 device key generation verified on real Android hardware 2026-09-10
- **Setup:** Task 2 complete (need the sandbox to enroll against later, though this task itself doesn't call it yet).
- **Work:** `flutter create` done — project `roster_vault` (org `com.rostervault`), targeting Android/iOS/Windows. Real home screen (`DeviceDebugScreen`, not the default counter demo). Nigel confirmed `biometric_signature` (researched and proposed below) — added to `pubspec.yaml` and implemented in `lib/services/device_identity_service.dart`: generates an ECDSA P-256 device keypair (alias `roster_vault_device_key`, `requireAuthentication: false`) and a random 16-byte `deviceId`, both created once and persisted (`flutter_secure_storage` for the deviceId; the plugin's own Keystore/Secure-Enclave storage for the keypair). Wired into `DeviceDebugScreen` to display both.
- **Correctness fix found during implementation:** the plugin's `createKeys` silently *replaces* an existing key under the same alias unless the caller checks first (`failIfExists` defaults to `false`). The service always calls `getKeyInfo` before `createKeys` for exactly this reason — documented in the service's doc comment and in [[../Security/Trust Anchors|Trust Anchors]].
- **Verify so far:** `flutter analyze` and `flutter test` both clean/passing. `flutter build windows --debug` succeeds. **Actually launched and screenshotted running** (screenshot sent to Nigel): shows "Roster Vault — Device Debug", a real `deviceId` (`920f031a5fa1edb1dd72a466aaedd396`), and a real public key in PEM format. **Relaunched and confirmed idempotent** — second launch showed the identical deviceId and public key, i.e. the key was reused, not regenerated. On the very first launch this took long enough (30s+) that it looked hung; added a 15-second timeout with a clear on-screen error message as a defensive improvement regardless (a real device screen should never spin silently forever), but the underlying cause turned out to be first-time TPM/Keystore provisioning latency, not a bug — every launch since has resolved in a few seconds. **Important caveat, confirmed not just theorized:** the returned public key is RSA-2048 SPKI (`MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8A...`), exactly matching the plugin's documented Windows-always-uses-RSA fallback — so this Windows run proves the UI/storage/idempotency wiring but genuinely cannot verify the ECDSA P-256 guarantee.
- **Update (2026-09-10, live session with Nigel, real Android hardware reached):** Nigel connected a real Samsung Galaxy A06 (Android 16 / API 36) — see the session's Plan Changes Log entries below for the wireless/USB adb connectivity work this took. Real sign-in and navigation to `DeviceDebugScreen` succeeded on real hardware. **But `ensureDeviceIdentity()` itself does not yet work there either** — hit a different, new blocker: `biometric_signature` 13.0.0's `createKeys()` fails with `BiometricError.unknown "Foreground activity required"`, reproducibly, even though the app is confirmed foregrounded and drawn. This is a real third-party plugin bug, root-caused by reading the plugin's own source, not a re-run of the Windows RSA-fallback issue. Full diagnosis and a temporary fix (pinning `biometric_signature` to an unreleased git commit past 13.0.0) are documented in [[../Troubleshooting/Known Issues|Known Issues]] and [[../Security/Trust Anchors|Trust Anchors]]. **Update (2026-09-10, continued): the fix was tested and confirmed NOT to work.** Nigel approved the `flutter run` launch that the permission classifier had blocked (ran it directly rather than through the driver script). Rebuilt with the git-pinned commit (32.1s build — confirmed a genuine recompile, not a cached no-op), reinstalled on the same real device: **identical failure, same error, same location.** That commit fixes a Gradle/AGP9 build-configuration problem, not the runtime `ActivityAware`/null-`activity` bug — a different issue that happened to live near the same code. Reverted `pubspec.yaml` back to the hosted `^13.0.0` (no reason to carry the git-pin risk for zero benefit). Searched the plugin's GitHub issues for this exact error and more broadly for "activity" — nothing matches. So: **the ECDSA P-256 guarantee remains unverified on real hardware**, and unlike the earlier update, this is now a *confirmed dead end* for this specific fix rather than a pending verification — don't re-attempt the git-pin without new evidence it would help.
- **Update (2026-09-10, resolved): the real root cause was never the plugin — it was a missing required setup step in this project's own `MainActivity.kt`.** Rereading the plugin's `README.md` (not just its source) turned up the actual documented requirement, verbatim: *"This plugin requires the use of a `FragmentActivity` instead of `Activity`. Update your `MainActivity.kt` to extend `FlutterFragmentActivity`."* `android/app/src/main/kotlin/com/rostervault/roster_vault/MainActivity.kt` had been left extending Flutter's default `FlutterActivity` since Task 3's original scaffolding — never updated per this plugin's own setup instructions. `BiometricSignaturePlugin.kt` stores its Activity reference typed as `FlutterFragmentActivity?`, populated via `binding.activity as? FlutterFragmentActivity` (a safe cast) — against a plain `FlutterActivity` that cast always silently returns `null`, exactly matching every symptom observed (activity genuinely attached and drawn, field still null). Changed `MainActivity.kt` to extend `FlutterFragmentActivity`, rebuilt, reinstalled on the same real device. **Confirmed working:** real ECDSA P-256 device key generated — Device ID `fe8888db42d5c63fddb42e91291a0481`, public key `MFkwEwYHKoZIzj0CAQYIKoZIzj0DAQcD...` (short SPKI, EC/P-256 OID prefix — distinct in shape and length from the Windows RSA-2048 SPKI). Full writeup in [[../Troubleshooting/Known Issues|Known Issues]] (now titled RESOLVED) and [[../Security/Trust Anchors|Trust Anchors]]. This also unblocked Task 4c's real on-screen enrollment milestone — see that task's entry.
- **Save:** Flutter project scaffold + device identity service exist and are fully verified — real ECDSA P-256 device keypair generation confirmed working on real Android hardware (Samsung Galaxy A06, Android 16/API 36), in addition to the earlier Windows dev-convenience run (which proved the UI/storage/idempotency wiring but could only produce an RSA fallback key). Both platforms now demonstrated.
- **Depends on:** Task 2
- **Review Agent (self-run):** No API in `biometric_signature` ever returns a private key — only public keys and signatures are exposed to Dart, so there's no code path that could log/serialize one. `requireAuthentication: false` on the device key is intentional (Trust Anchor 2 isn't gated by a person's factor; Trust Anchor 3 in Task 6 will use the same plugin with it turned on, under a different alias). Verdict: safe to proceed; real-device signature-type verification is a test-coverage gap, not a safety flag.
- **Plugin research (resolved):** Flutter has no built-in hardware-backed asymmetric key generation. Nigel chose "vet a maintained plugin first," then confirmed the recommendation. Rejected candidates: `keystore_plugin` (supports it, but last published ~12 months ago, 1 like — under-maintained), `flutter_secp256r1` (pub.dev page 404s, couldn't verify), `biometric_crypto`/`synheart_auth` (not vetted in depth once a strong candidate emerged). **Chosen: `biometric_signature`** — verified publisher (visionflutter.com), actively maintained (v13, published 46 days prior), 160 pub points / 54 likes / 28.7k downloads, MIT license, supports multiple named keys via `keyAlias` (reusable for Task 6's per-user keys).

## Task 4: Enrollment flow, end to end (online)
- **Status:** NOT STARTED — scoped and sub-decomposed 2026-09-09, not yet approved to start
- **Setup:** Tasks 2 and 3 complete.
- **Scoping note (2026-09-09):** Task 4 alone crosses 5 layers (Flutter → Cognito → AppSync → Lambda → KMS → DynamoDB), adds new crypto wiring and new IAM surface, and has well over 3 acceptance criteria — too big for one sitting per the Story Agent's own rubric. Split into 4a/4b/4c below, mechanism before policy. **Scoping decision confirmed with Nigel:** Task 4c stores the enrollment token via `flutter_secure_storage`'s own OS-level encryption only — no PIN wrapping yet. The [[../Architecture/Enrollment Flow|Enrollment Flow]] diagram's steps 6–7 (PIN/biometric setup, PIN-wrapped encryption) are real but deferred to Task 6 as originally scoped in this checkpoint, not pulled forward — keeps Task 4 reviewable in one sitting and separates mechanism from policy.

### Task 4a: Enrollment Lambda + AppSync mutation (backend only, no UI)
- **Status:** COMPLETE
- **Work:** Added `amplify/appsync/schema.graphql` (single `enroll` mutation, Cognito User Pool-authorized) and `amplify/functions/enroll/{resource.ts,handler.ts}`. Handler reads any existing `DeviceEnrollments` row (preserving epoch across re-enrollment), writes `{deviceId, userId, devicePubKey, userPubKey, epoch, status: active, lastIssuedExpiry}`, hand-builds a standard ES256 JWS (`{user_id, device_id, scopes: [offline_signin], epoch, iat, exp}` at ~48h TTL) signed via `kms:Sign`, converting KMS's DER signature to JWS's raw 64-byte R‖S format. Baked the issuer's public key into the app as `lib/services/issuer_public_key.dart` (checked in, with its own regeneration command). Full detail in [[../Backend/Backend Index|Backend Index]].
- **Verify:** Created a real Cognito test user (`test-task4a@rostervault.dev`), obtained a real ID token (`admin-initiate-auth`), called `enroll` over HTTPS against the deployed sandbox AppSync endpoint, got back a signed JWT, and verified its signature locally in Node (`crypto.verify` with `dsaEncoding: 'ieee-p1363'`, against the exact PEM baked into the app) — **signature valid**, 64-byte raw signature as expected for P-256, correct claim shape. Confirmed via direct `GetItem` that the `DeviceEnrollments` row was written correctly. Re-called `enroll` a second time after tightening the DynamoDB grant (see Review Agent below) — still works, and `epoch` stayed `0` across both calls, confirming the read-preserves-epoch logic. No Flutter UI involved, as scoped.
- **Save:** Enrollment Lambda deployed to sandbox, callable, producing verifiably-signed tokens.
- **Files changed:** `amplify/appsync/schema.graphql` (new), `amplify/functions/enroll/resource.ts` (new), `amplify/functions/enroll/handler.ts` (new), `amplify/backend.ts`, `lib/services/issuer_public_key.dart` (new), `test/widget_test.dart` (unrelated pre-existing bug fixed, see Plan Changes Log), `package.json`/`package-lock.json` (added `@aws-sdk/client-dynamodb`, `@aws-sdk/util-dynamodb`, `@types/node`), `docs/roster-vault/Backend/Backend Index.md`, `docs/roster-vault/Security/Trust Anchors.md`.
- **Depends on:** Task 2.
- **Review Agent (self-run, full checklist against `amplify/backend.ts`, `amplify/functions/enroll/*`, `amplify/appsync/schema.graphql`):**
  - ✅ Key material never leaves its boundary — handler only ever calls `kms:Sign` (never a private-key-export API); the token returned to the caller is the intended deliverable, not leaked secret material; no logging of tokens/keys in the handler.
  - ✅ Token/epoch correctness (issuance side) — epoch is read-then-preserved, never reset by self-re-enrollment; only an explicit admin action (Task 8) will ever bump it.
  - ✅ Environment leakage — no dev/test values hardcoded in Lambda code; the CLI test user/password used for verification live only in shell history and gitignored `.artifacts/`, never committed.
  - ✅ Missing environment variables — `ISSUER_SIGNING_KEY_ID` and `DEVICE_ENROLLMENTS_TABLE_NAME` both set and both documented in Backend Index.
  - ✅ Cross-account/region — no hardcoded region or account id introduced; `KMSClient({})`/`DynamoDBClient({})` pick up the Lambda's own execution region.
  - ✅ Secrets and credentials — no secrets committed; KMS key ARN/id treated as identifiers, not secrets (consistent with Task 2's own precedent).
  - ⚠️ **IAM scope creep, found and fixed:** initial implementation used `deviceEnrollments.grantReadWriteData(enrollFn)`, which grants `Query`/`Scan`/`DeleteItem`/`BatchWriteItem`/`BatchGetItem` the handler never calls. Tightened to `deviceEnrollments.grant(enrollFn, 'dynamodb:GetItem', 'dynamodb:PutItem')` — exactly what's used. Redeployed and re-verified working after the fix.
  - ⚠️ **Task 4a's own stated scope not fully done on first pass, found and fixed:** the issuer public key had been fetched ad hoc for CLI verification but not actually baked into the app as originally scoped. Added `lib/services/issuer_public_key.dart` before considering the task complete.
  - **Verdict:** safe to proceed, both flagged items resolved before this commit (not merely noted-and-deferred).
- **Checkpoint saved:** 2026-09-09

### Task 4a note: unrelated pre-existing test bug found and fixed
`flutter test` failed (`A Timer is still pending even after the widget tree was disposed`) before any Task 4a work touched `main.dart` or the test — `DeviceDebugScreen.initState`'s 15-second timeout (Task 3) creates a real `Timer` that a widget test's single `pumpWidget` never advances past, since there's no real platform channel behind `biometric_signature` in a test environment for the underlying future to resolve against. `testWidgets` already runs inside a fake-async zone, so `test/widget_test.dart` now pumps an extra 16 seconds after its assertions to let that timer fire before the test ends, rather than leaving a stray pending timer. Not part of Task 4a's own scope, but left broken would have violated the Story Agent's "task must end in a stable state" rule — fixed rather than left red. This means Task 3's "flutter test... passing" claim was stale/inaccurate at the time it was written; corrected here.

### Task 4b: Flutter Cognito login screen
- **Status:** COMPLETE
- **Work:** Added `amplify_flutter` + `amplify_auth_cognito`. `lib/main.dart` now bootstraps Amplify (`_AuthGate`: loads `amplify_outputs.json` from the asset bundle, `Amplify.addPlugins([AmplifyAuthCognito()])`, `Amplify.configure(...)`) and routes to `lib/screens/login_screen.dart` when signed out, or `DeviceDebugScreen` (now showing "Signed in as: &lt;sub&gt;") when a session already exists. `LoginScreen` is a plain email/password form calling `Amplify.Auth.signIn`.
- **Verify:** Screenshotted the real running Windows app landing cleanly on the login screen (Amplify bootstrap succeeded — no error screen). Then, since there's no literal "real person" available in autonomous execution, drove the actual rendered window with real Win32 mouse clicks + `SendKeys` keyboard input (not a mock, not a widget test — the genuine compiled `roster_vault.exe`): clicked into the Email field, typed the Task 4a test user's address, clicked Password, typed its password, clicked Sign In. Screenshotted the result: the app transitioned to `DeviceDebugScreen` showing **"Signed in as: 296ef4f8-90f1-704c-6cc4-23f1d53dcd78"** — the exact Cognito `sub` of that test user — proving a real SRP sign-in round trip against the deployed User Pool succeeded and the UI correctly reflects the authenticated state. Screenshots: `.claude/skills/run-roster-vault/.artifacts/task4b-login-verify/{1_login_screen,2_filled_form,3_after_signin}.png` (gitignored, not committed).
- **Save:** Working login screen, authenticated session obtainable, verified against the real deployed pool.
- **Depends on:** Task 2. (Independent of 4a — order between 4a/4b doesn't matter; both must land before 4c.)
- **Note on the verify method:** the checkpoint's original bar ("a real person taps through") isn't literally satisfiable by an autonomous agent with no human present. Win32 input simulation on the actual compiled window was chosen over a `flutter test` widget-pump alternative specifically because Amplify Auth may touch real platform-channel plugins (secure storage) during sign-in that don't exist in a plain widget test's sandbox — driving the real window avoids that risk of a misleading result. Judgment call under the relaxed decision-point rule, logged in the Plan Changes Log.
- **Also fixed:** `test/widget_test.dart` no longer worked as-is once `RosterVaultApp`'s `home` became `_AuthGate` instead of `DeviceDebugScreen` directly — `_AuthGate` calls `Amplify.configure()`, which isn't safely testable in a plain widget test. Split into two tests, each pumping the relevant screen directly (`DeviceDebugScreen`, `LoginScreen`) rather than the whole app through its real (platform-channel-dependent) entry point.
- **Files changed:** `pubspec.yaml`/`pubspec.lock` (added `amplify_flutter`, `amplify_auth_cognito`, `amplify_outputs.json` as a bundled asset), `lib/main.dart` (`_AuthGate`, `DeviceDebugScreen.signedInUser`), `lib/screens/login_screen.dart` (new), `test/widget_test.dart`, `windows/flutter/generated_plugin_registrant.cc` + `generated_plugins.cmake` (auto-generated, new `amplify_db_common` Windows plugin registration).
- **Checkpoint saved:** 2026-09-09

### Task 4c: Wire it end to end + local storage of the result
- **Status:** COMPLETE — full on-screen milestone achieved on real Android hardware 2026-09-10 (see update below); mechanism was already fully built and verified before that, blocked only by a Windows-specific environment gap that doesn't apply on the real deployment target
- **Work:** Added `lib/services/user_identity_service.dart` (generalizes Task 3's `device_identity_service` pattern to a parameterized per-user key alias, Trust Anchor 3), `lib/services/enrollment_service.dart` (calls the Task 4a `enroll` mutation with the live Cognito ID token from Task 4b's session, stores the result via `flutter_secure_storage` — placeholder per the earlier scoping decision), and `lib/services/token_verifier.dart` (verifies the returned JWS locally against the Task 4a-baked issuer public key). Wired into `DeviceDebugScreen` as an "Enroll this device" button with an "Enrollment" debug section showing signature-valid status and decoded claims.
- **Verify — what's actually proven, precisely:**
  - **Real, working, on the real deployed backend:** driving the actual running app (same Win32-input method as Task 4b) through sign-in and clicking "Enroll this device" *did* correctly disable the button and enter the loading state — proving the button's `onPressed` wiring and `_isEnrolling` state machine are correct.
  - **Genuinely blocked, not faked:** the click never completed. Diagnosed by enumerating all system windows during the hang: three orphaned `CredentialUIBroker.exe` processes ("Windows Security" in the window list, invisible in any screenshot since they render off the captured window) — `biometric_signature`'s native key-creation call is waiting on a Windows Hello consent prompt that can never resolve, because **no Windows Hello PIN is configured on this machine**. This is the same class of environment gap already documented in Task 3 for the device key (Trust Anchors.md's existing caveat: "requires a Windows Hello PIN to be configured on this machine"), now hit by the *user* key alias specifically because it's the first time that particular code path ever executed (the device key's alias already existed from earlier sessions, so its own `createKeys` call was never re-triggered in any of this session's runs). Per the relaxed device/hardware stop condition: documented honestly here, the orphaned broker processes were cleaned up, and everything *not* strictly requiring this one native call was still verified (below) rather than the whole task being blocked.
  - **Verified independently, against the real deployed backend:** `EnrollmentService`'s exact mutation-call code path was already proven correct in Task 4a's CLI verification (identical GraphQL query/variables shape, real Cognito token, real signed response) — the only genuinely new client-side logic Task 4c adds on top of that is `TokenVerifier`. That's tested directly (`test/token_verifier_test.dart`, pure Dart, no Amplify/platform channels) against a real KMS-signed token captured from Task 4a's own verification: signature valid, correct claims decoded, and a tampered-payload variant is correctly rejected.
  - **A real dead end found and fixed along the way:** the first `TokenVerifier` implementation used `package:cryptography`'s `Ecdsa.p256(Sha256())`, hand-rolling JWS parsing and DER→raw signature conversion. It failed with `UnimplementedError` — `package:cryptography`'s pure-Dart P-256 implementation is a stub; every method, including `verify`, unconditionally throws (`cryptography-2.9.0/lib/src/dart/ecdsa.dart`). Rewritten using `dart_jsonwebtoken` instead, which docs/roster-vault/Backend/Tech Stack Mapping.md had actually specified for this exact job from the start — the `cryptography` detour was an unforced deviation from the already-approved stack, not a case of the approved stack being wrong. `dart_jsonwebtoken` verifies via `pointycastle` underneath, a real pure-Dart P-256 implementation with no platform channel of any kind, so it also can't hit the Windows-Hello-style problem.
  - **Defensive improvement added on the same principle as Task 3's existing device-key timeout:** `_enroll()` now wraps `ensureUserKeyPair` in the same 15-second-timeout-with-clear-message pattern DeviceDebugScreen already uses for the device key, so a future real user hitting this exact hang gets an on-screen explanation instead of an infinite spinner.
- **Update (2026-09-10, resolved on real Android hardware):** the Windows-only blocker above (missing Windows Hello PIN) turned out to be moot once Task 3's real root cause was found and fixed — `MainActivity.kt` needed to extend `FlutterFragmentActivity`, not `FlutterActivity` (see Task 3's entry and [[../Troubleshooting/Known Issues|Known Issues]]). With that fixed, the full literal click-through was observed on real hardware, in one continuous run, with Nigel present: real sign-in on a real Samsung Galaxy A06 → tap "Enroll this device" → **Signature: VALID**, with full decoded claims shown on screen (`user_id`, `device_id`, `scopes: [offline_signin]`, `epoch`, `iat`). This satisfies the task's original bar in full — a valid, correctly-signed offline token, stored locally, inspectable in a debug view, confirmed against the real deployed sandbox.
- **Save:** Enrollment mechanism fully built, backend-verified, unit-tested, and now confirmed end-to-end on real Android hardware with a real enrolled profile.
- **Files changed:** `lib/services/user_identity_service.dart` (new), `lib/services/enrollment_service.dart` (new), `lib/services/token_verifier.dart` (new, rewritten mid-task from a `package:cryptography`-based dead end), `lib/main.dart` (enrollment UI + timeout; later also `DeviceDebugScreen`'s body wrapped in `SingleChildScrollView` — the plain `Column`/`Padding` overflowed off-screen on the real device's resolution once the Enrollment and Offline Sign-In sections were both visible, confirmed via a "BOTTOM OVERFLOWED BY 202 PIXELS" debug banner), `android/app/src/main/kotlin/com/rostervault/roster_vault/MainActivity.kt` (extends `FlutterFragmentActivity`, see Task 3), `pubspec.yaml`/`pubspec.lock` (added `dart_jsonwebtoken`; `cryptography`/`http` remain from the earlier attempt — `cryptography` is still needed for Task 6's Argon2id, `http` for `EnrollmentService`), `test/token_verifier_test.dart` (new).
- **Review Agent (self-run):**
  - ✅ Key material never leaves its boundary — `UserIdentityService`/`biometric_signature` never expose a private key to Dart (same guarantee already reviewed for the device key in Task 3); `EnrollmentService` stores the issued *token*, not private key material; `TokenVerifier` only ever handles the issuer's *public* key.
  - ✅ Per-user data isolation (mechanism-level) — the stored token's storage key is namespaced `roster_vault_enrollment_token_<userId>`, consistent per enrolled person. Full HKDF-derived per-user partitioning is still correctly deferred to Task 6, per the earlier scoping decision.
  - ⚠️ **Judgment call, logged rather than silently decided:** `test/token_verifier_test.dart` commits a real, validly KMS-signed offline capability token as a fixture (captured from Task 4a's own CLI verification) rather than only a synthetic/mocked one. Reasoned acceptable: it's bound to a nonsense test `device_id` and a sandbox-only test Cognito user, expires in 48h, and — critically — grants no actual privilege today since Task 5's real verifier (the only thing that would ever consume such a token for real) doesn't exist yet. Flagging this explicitly rather than deciding it's obviously fine, given this project's own stated bar of treating tokens with the same care as credentials.
  - **Verdict:** safe to proceed. No private key material, no broadened grants, no shape changes to already-issued tokens.
- **Checkpoint saved:** 2026-09-10

## Task 5: Offline verification (the core hard part)
- **Status:** COMPLETE — full on-screen milestone achieved on real Android hardware 2026-09-10, including the device's network genuinely disabled (see update below)
- **Setup:** Task 4 complete — at least one enrolled profile with a stored token exists. **Not actually true on this machine** (Task 4c's blocker), which is exactly why the on-screen milestone below can't be done here — see Task 4c.
- **Work:** Added `lib/services/offline_verifier.dart` implementing every step of [[../Architecture/Offline Sign-In Flow|Offline Sign-In Flow]] steps 4-6 precisely as specified there and in [[../Security/Token Lifecycle and Revocation|Token Lifecycle and Revocation]]: signature check (via `dart_jsonwebtoken`, reusing Task 4c's fix), expiry check, device-id check, epoch check, and challenge-response (a fresh local nonce signed with the person's private key, verified against their stored public key). Extended `EnrollmentService` to separately persist the on-device "known epoch" at each successful enroll/refresh — re-reading Token Lifecycle and Revocation.md caught that the epoch check must compare against *that*, not the token's own epoch claim (which would make the check a tautology that always passes). `OfflineVerifier` makes zero network calls by construction (no `http`/Amplify import at all, not just "reviewed to not call the network").
- **Verify:**
  - **Rigorously proven, with real cryptography:** `test/offline_verifier_test.dart`, 11 table-driven tests against real openssl-generated P-256 keypairs (not mocked crypto) — every failure mode independently (bad signature, expired, wrong device, stale epoch, challenge signed by the wrong key, challenge echoing the wrong nonce) plus two meaningful combinations, confirming the exact short-circuit precedence order. Also explicitly tests the epoch comparison *direction* both ways (equal and newer epochs must NOT be rejected) — this is precisely the one-character-bug class the Review Agent checklist calls out by name, and it's covered, not just claimed correct by inspection.
  - **UI wired, typechecked, but the specific live demo is genuinely not achieved — two independent reasons, not glossed over:**
    1. **Same root blocker as Task 4c.** No real enrollment/user key exists on this machine (Windows Hello PIN not configured), so there is no genuine on-device token to run the debug "Sign in offline" button against yet.
    2. **A Win32 UI-automation harness anomaly, investigated but not fully root-caused.** Driving the app to click "Sign in offline" instead produced a spurious click on the unrelated "Enroll this device" button (confirmed reproducible across two clean, independently-launched runs with fresh PIDs — not a stale-process artifact). Tried and ruled out: DPI-scaling mismatch between `SetCursorPos`/`GetWindowRect` and `PrintWindow` (checked actual system DPI: 96, i.e. 100% — not the cause); a stationary cursor left sitting over the *new* button's position after the screen transition (added cursor-parking after every click — did not fix it). Rather than keep spending turns on what is a test-harness question, not a production-code defect (the actual button wiring in `main.dart` follows the exact same pattern already proven working for the Enroll button in Task 4c), this was stopped and reported honestly instead of forcing a screenshot that wouldn't actually show what it claimed to.
    3. **Deliberately not attempted: disabling this machine's real network connectivity.** The shell tools this session uses to drive the app run *on the same machine* being tested — cutting its network access would sever this session's own ability to receive further tool results or send commands, with no way to reverse it once done. Considered a safer alternative (a Windows Firewall rule scoped to just `roster_vault.exe`'s process, leaving the rest of the machine including this session's connectivity untouched) but didn't pursue it, since there's no genuine enrolled token to test with yet regardless of network state.
  - **The production challenge-response signer is deliberately left unimplemented,** not guessed at: `biometric_signature`'s raw-signature output format (DER vs. the fixed-width IEEE P1363 `r‖s` format JWS needs — exactly the conversion Task 4a's Lambda had to do explicitly for KMS's own DER output) isn't documented, and guessing wrong would silently produce a challenge-response that always fails on real hardware. Throws a clear `UnimplementedError` pointing at this reasoning and at the test suite that proves the surrounding verification logic is correct independent of it.
- **Update (2026-09-10, real hardware, real stored token, still not fully complete):** with Task 3/4c's root cause fixed and a real enrollment on-device, the "no genuine on-device token" half of blocker #1 above is resolved — a real enrolled profile now exists on the test Android device. Tapped "Sign in offline (verify stored token)" for real: `OfflineVerifier` ran against the real stored token and correctly reached the challenge-response step, where it hit the still-deliberately-unimplemented `_productionChallengeSigner` — shown on screen as **`Result: FAIL (challengeFailed)`**, a normal typed `OfflineVerificationResult`, not a crash or unhandled exception. This is real, meaningful positive evidence: it confirms the verifier's signature/expiry/device-id/epoch checks all passed against a genuine on-device token before correctly stopping at the one intentionally-unimplemented step, and confirms the `verify()` contract surfaces a signing failure as a typed outcome rather than letting an exception escape to the UI. **Task 5 is still not complete** — a real PASS has never been shown, only a correctly-reached, correctly-typed FAIL at the expected step, since the production challenge signer remains unimplemented (same DER-vs-P1363 reasoning as before; not yet resolved by anything in this update). The Win32 UI-automation anomaly (#2) and the network-disabling risk decision (#3) were Windows-specific concerns that don't apply to this Android run — moot now that Android is the active platform for this testing, not newly resolved.
- **Update (2026-09-10, continued): production challenge signer implemented, real PASS achieved, airplane-mode milestone fully demonstrated.** The DER-vs-P1363 question was resolved definitively rather than guessed: read `biometric_signature`'s Android source directly (`BiometricSignaturePlugin.kt`) and confirmed it signs via plain `java.security.Signature.getInstance("SHA256withECDSA")` — standard JCA, always DER-encoded, no P1363 path exists anywhere in that plugin. Added `UserIdentityService.signChallenge()`: builds the ES256 JWS signing input (`{alg: ES256, typ: JWT}` header + `{nonce}` payload, matching the exact shape `amplify/functions/enroll/handler.ts` already uses), signs it via `biometric_signature.createSignature(signatureFormat: SignatureFormat.raw)`, and converts the returned DER bytes to the fixed-width 64-byte raw `r‖s` format JWS requires — a direct Dart port of the same `derToRawEcdsaSignature` algorithm already proven correct in the Lambda (same problem, same fix, ported rather than reinvented). `main.dart`'s `_productionChallengeSigner` now calls this instead of throwing. `flutter analyze`: clean.

  Rebuilt on the real Samsung Galaxy A06 and re-ran "Sign in offline": **`Result: PASS`** — first real, non-error pass through every check (signature, expiry, device-id, epoch, and now a genuine hardware-signed challenge-response), on real hardware, against a real KMS-issued token. Then, since a real device was in hand: enabled airplane mode via `adb shell cmd connectivity airplane-mode enable` (confirmed via `settings get global airplane_mode_on` returning `1`, and visually via the airplane icon replacing the Wi-Fi icon in the status bar), tapped "Sign in offline" again — **`Result: PASS` with the device's network genuinely, verifiably disabled.** This is the actual hard requirement, not just code that doesn't call the network, and not just a claim — directly satisfies the Story Agent's own directive ("never claim offline without proving it") and the original Task 5 Verify bar's "third on-screen milestone, with the device in airplane mode." Restored normal connectivity afterward (`airplane-mode disable`, confirmed back to `0`).

  Note on why this was safe to do via adb on this specific device, when Task 5's earlier entry had declined to cut network access for exactly this kind of test: that earlier caution was about *this development machine's own* network (the shell tools driving the app run on the same Windows PC being tested, so cutting its network risked severing the session's own ability to continue). The Android phone's airplane mode is a completely separate device, reached only over its USB cable — disabling its Wi-Fi/cellular radios has no effect on this PC's own connectivity or this session's ability to keep issuing `adb` commands, so the original risk doesn't apply here.
- **Save:** The actual hard requirement — offline signature/expiry/device-id/epoch/challenge-response verification — is built, proven correct against real cryptography (11 table-driven tests), and now demonstrated end-to-end on real Android hardware with a real enrolled profile, a real hardware-backed challenge-response signature, and the device's network genuinely disabled. Nothing outstanding for this task.
- **Depends on:** Task 4 (see caveat above).
- **Spawns:** [[../../agents/test-agent/test-agent|Test Agent]] — done: the 11 table-driven tests above satisfy this directive.
- **Files changed:** `lib/services/offline_verifier.dart` (new), `test/offline_verifier_test.dart` (new, 11 tests), `lib/services/enrollment_service.dart` (persist known epoch separately), `lib/main.dart` (Offline Sign-In debug section + `_productionChallengeSigner`).
- **Review Agent (self-run — this task is squarely "any code that constructs, signs, or verifies a token"):**
  - ✅ Signature/expiry/device-id/epoch — all four checked, not a subset; comparison direction on epoch specifically tested both ways (this is the exact bug class the checklist names).
  - ✅ No network call anywhere in `OfflineVerifier` — enforced by construction (no `http`/Amplify import), not merely "reviewed and believed."
  - ✅ Challenge-response correctly proves possession of the private key, not just the token blob — tested with a token that's valid but a challenge signed by the *wrong* key, and separately a challenge that echoes the *wrong* nonce (a replay of a stale response).
  - ✅ **Update (2026-09-10): production challenge signer implemented and reviewed.** `UserIdentityService.signChallenge()` never touches a private key from Dart — it only ever receives back signature bytes and the already-public public key from `biometric_signature`, exactly the same guarantee already reviewed for device-key and user-key generation. The DER-to-raw conversion is a direct, unmodified port of the Lambda's own already-reviewed algorithm (Task 4a), not new crypto design. Resolved by reading the plugin's actual source rather than guessing — the stated risk ("guessing wrong would silently produce a challenge-response that always fails on real hardware") was exactly the reason this waited for hardware access instead of shipping a guess.
  - **Verdict:** safe to proceed, and this task is now fully complete — verification logic, real-hardware challenge signing, and the airplane-mode demonstration are all done, not merely native-plugin wiring outstanding.
- **Checkpoint saved:** 2026-09-10

## Task 6: Local unlock factor and per-user encrypted storage
- **Status:** COMPLETE — real PIN gate, real two-profile isolation, demonstrated on real Android hardware 2026-09-10 (see final update below)
- **Setup:** Task 5 complete (mostly — see its own caveats).
- **Work:** Added three services. `lib/services/pin_service.dart` — Argon2id-derived AES-256-GCM key wrapping for the enrollment token (the "PIN entry ... key wrapping" the checkpoint asked for; deliberately doesn't store a separate PIN hash — correctness is proven by whether the authenticated cipher successfully decrypts, which also means a wrong PIN fails loudly rather than needing a second stored artifact that would itself be a crackable target). `lib/services/device_master_key_service.dart` — a dedicated random 256-bit `K_device`, since the architecture docs specify the *formula* `K_user = HKDF(K_device, user_id)` precisely but not what `K_device` concretely is; deliberately not reusing the Trust Anchor 2 ECDSA device key as raw HKDF input (mixing a signing key into a symmetric KDF is a known crypto smell). `lib/services/partition_key_service.dart` — the HKDF derivation itself, exactly as specified in [[../Architecture/Multi-User Partitioning|Multi-User Partitioning]]. `lib/services/encrypted_partition_store.dart` — intended to open a per-user SQLCipher-encrypted SQLite file keyed by `K_user`.
- **A real, load-bearing finding, caught rather than missed:** `sqlcipher_flutter_libs`'s Windows build requires OpenSSL dev headers, not installed on this machine, and installing system-wide dev tools is outside this project's authorized scope (this repo and its own Amplify backend only). Adding the dependency **broke the Windows build entirely** (`flutter build windows` failed at the CMake step). Reverted it immediately. Then verified — rather than just assumed — that without it, `PRAGMA key` on `package:sqlite3` is a silent no-op: `PRAGMA cipher_version` returns no rows (no cipher support compiled in at all), and a file written through `EncryptedPartitionStore` starts with the literal, unencrypted `"SQLite format 3\0"` header. The project's own test suite caught this concretely: a "wrong key must fail to open an existing partition" test unexpectedly passed with the *wrong* key too, because with no real cipher active there is no way to distinguish a correct key from an incorrect one. That test is now `skip:`-marked with the reason inline, rather than deleted or left silently red — see `test/pin_and_partition_test.dart`.
- **Verify — proven with real cryptography, no platform/native dependency involved:**
  - Argon2id → AES-GCM wrap/unwrap: correct PIN unwraps the original plaintext; wrong PIN returns `null` (not garbage, not a thrown error) via the cipher's own authentication check; wrapped data survives a JSON round-trip (as it would through actual storage).
  - HKDF partition-key derivation: same device key + user id always derives the same partition key (determinism, needed for it to be usable at all); different users on the same device get different, unrelated keys.
  - All smoke-tested directly with `dart run` before being built into services, after the Task 4c/5 lesson about `package:cryptography` having entirely-unimplemented algorithms (Ecdsa) alongside genuinely-working ones (Argon2id, HKDF, AES-GCM all independently confirmed real before use).
- **Verify — NOT achieved, honestly:** the fourth on-screen milestone (PIN pad UI, cross-profile isolation demonstrated by attempting cross-access) was not built this round. Building a PIN entry screen and roster-adjacent UI on top of a partition-encryption mechanism just discovered to be non-functional on this machine would have meant demonstrating something that either fakes success or visibly doesn't do what it claims — neither is acceptable. The mechanism (Argon2id/HKDF/AES-GCM) is real and tested; the SQLCipher-backed encrypted storage it's meant to protect is not yet real here.
- **Update (2026-09-10, continued, live session with Nigel): real encryption resolved on both platforms.** `sqlcipher_flutter_libs` (the obvious next attempt) turned out to be EOL upstream — its own pub.dev listing (latest `0.7.0+eol`) says outright "Not used anymore, update to version 3.x of package:sqlite3 instead." Followed that: `package:sqlite3` 3.x bundles cipher support natively via Dart's native-asset build hooks, with three selectable native binary variants (`sqlite3.dart`'s `doc/hook.md`). Chose `sqlite3mc` (SQLite3 Multiple Ciphers) deliberately over `sqlcipher` — the docs state the `sqlcipher` build itself "links OpenSSL on Windows, Linux and Android," the exact blocker this was trying to escape, while `sqlite3mc` is self-contained with no OpenSSL dependency, shipped as a precompiled binary per platform. Added `hooks.user_defines.sqlite3.source: sqlite3mc` to `pubspec.yaml`.

  Upgrading `drift` to `^2.34.0` (first version compatible with `sqlite3` 3.x) surfaced a real pub version-solving conflict: `amplify_auth_cognito` 2.11.0's transitive chain pinned `sqlite3 ^2.7.6`, traced back to `flutter_secure_storage ^9.2.4`'s own old `win32` pin. Resolved by bumping `flutter_secure_storage` to `^11.0.0` and `amplify_flutter`/`amplify_auth_cognito` to `^2.15.0` together. That, in turn, surfaced two independent real Android build failures, both fixed properly: `flutter_secure_storage` 11 requires `compileSdk 37` (Flutter's own default was still 36) — fixed by hardcoding `compileSdk = 37` in `android/app/build.gradle.kts`; then a stale `GeneratedPluginRegistrant.java` left over from before the bump — fixed with `flutter clean` + `flutter pub get`, not a manual patch. Also hit a real, self-healing runtime quirk: existing on-device values encrypted under the old `flutter_secure_storage` cipher failed to decrypt once, then the plugin's own new version deleted and regenerated them automatically rather than crashing (confirmed via a fresh Device ID appearing on the next run) — see [[../Troubleshooting/Known Issues|Known Issues]] for full detail on all of the above.

  **Verified real encryption on Windows first:** a standalone `dart run` script wrote real data through `EncryptedPartitionStore`, confirmed the raw file header is not the plaintext SQLite magic, the plaintext value doesn't appear anywhere in the raw bytes, a wrong key fails to read existing data, and the right key still works. The previously-`skip:`-marked "wrong key" test in `test/pin_and_partition_test.dart` now genuinely passes (un-skipped, not deleted). **Then verified on the real Android deployment target**, same discipline as Task 5's airplane-mode proof: added a "Test encrypted storage" section to `DeviceDebugScreen` (mirrors the Enrollment/Offline Sign-In sections' pattern) that runs the identical write/wrong-key/right-key check live in the running app. Rebuilt on the same real Samsung Galaxy A06, tapped it: **`Result: PASS — file header not plaintext, secret not found in raw bytes, wrong key rejected, right key read the real value back`.** `flutter analyze`: clean. `flutter test`: 23/23 passing. `flutter build windows --debug`: still succeeds — the whole dependency bump didn't break the Windows dev-convenience path either.

  **Update (2026-09-10, final, live session with Nigel): the PIN pad and two-profile isolation demonstration, built and proven end to end.** Added `lib/services/local_unlock_service.dart`, tying together the already-proven pieces. **A real, documented design decision, not silently assumed:** the architecture's literal `K_user = HKDF(K_device, user_id)` formula has no PIN in it — anyone who can read `K_device` (only OS-encrypted, not PIN-gated) can derive the same `K_user` without ever entering a PIN, which is in tension with Multi-User Partitioning.md's own prose ("can't read another profile's data without that profile's own local factor"). Resolved by giving the PIN a genuine, separate job: it PIN-wraps the enrollment token itself (via the already-proven `PinService`, Argon2id-derived — real key material, not a UI-only gate) as a row *inside* the K_user-encrypted partition, rather than being mixed into the database key. The two layers now cover both named threats: raw file theft alone (needs `K_device`) and device compromise alone (needs the PIN) both fail; only both together succeed. Documented in `LocalUnlockService`'s class doc comment, not just here.

  Added a "Local Unlock (Task 6)" section to `DeviceDebugScreen` (PIN field + Set PIN/Unlock button, same pattern as the Enrollment/Offline Sign-In/Storage-Test sections) and a `Sign Out` action (new, needed to actually switch between two real profiles — not Task 7's real roster/fast-switch UI, just enough to prove isolation now). Created a second real Cognito test user (`test-task6b@rostervault.dev`) via `aws cognito-idp admin-create-user` + `admin-set-user-password`, same pattern as Task 4a's first test user.

  **Verified live on the real Samsung Galaxy A06, every step actually observed on screen, not inferred:**
  1. Signed in as `test-task4a`, re-enrolled (the earlier `flutter_secure_storage` migration had wiped the previously-stored token too, not just the device key — re-enrolled for real, **Signature: VALID** again).
  2. Set PIN `1234` for this profile — UI correctly switched from "Set PIN" to "Unlock" mode afterward.
  3. Unlocked with `1234`: **`PASS — unlocked with the correct PIN, recovered token: eyJhbGciOiJFUzI1NiIs...`** — a genuine JWT prefix, the real recovered token, not a placeholder.
  4. Tried a wrong PIN (`9999`): **`Wrong PIN — rejected.`**
  5. Signed out (real `Amplify.Auth.signOut()`, not simulated), signed in as `test-task6b`, enrolled that device+user pair fresh — **Signature: VALID**, correct (different) `user_id` claim.
  6. Set a *different* PIN (`5678`) for this second profile — UI correctly showed "Set PIN" again (fresh, independent of profile 1's already-set-up state).
  7. **The actual isolation test:** entered profile 1's PIN (`1234`) against profile 2's unlock screen — **`Wrong PIN — rejected.`** Cross-profile access genuinely blocked, not assumed.
  8. Entered profile 2's own correct PIN (`5678`): **`PASS — unlocked with the correct PIN...`** — their own, different token recovered correctly.

  This is the full "fourth on-screen milestone" the checkpoint originally asked for: a real PIN pad, two enrolled profiles on one device, cross-access attempted and blocked, wrong PIN visibly rejected, correct PIN unlocking — all real, all observed, none of it inferred from code review.
- **Save:** Real per-user encrypted storage, a real PIN gate wrapping each profile's own token, and real cross-profile isolation are all proven end to end on the actual deployment target. Nothing outstanding for this task.
- **Depends on:** Task 5
- **Files changed:** `lib/services/pin_service.dart` (new), `lib/services/device_master_key_service.dart` (new), `lib/services/partition_key_service.dart` (new), `lib/services/encrypted_partition_store.dart` (new; class doc comment rewritten once encryption was confirmed active, `dispose()`→`close()` for sqlite3 3.x), `lib/services/local_unlock_service.dart` (new), `test/pin_and_partition_test.dart` (new, then the "wrong key" test un-skipped once real encryption was confirmed), `lib/main.dart` (new "Encrypted Storage" and "Local Unlock" debug sections, `Sign Out` action, `_onSignedOut`), `android/app/build.gradle.kts` (`compileSdk = 37`), `pubspec.yaml`/`pubspec.lock` (`drift` → `^2.34.0`, `sqlite3` → `^3.4.0`, `flutter_secure_storage` → `^11.0.0`, `amplify_flutter`/`amplify_auth_cognito` → `^2.15.0`, `path_provider` added, `hooks.user_defines.sqlite3.source: sqlite3mc` added; `sqlcipher_flutter_libs` tried once more and not kept, see Known Issues).
- **Review Agent (self-run, update — this task now also touches the actual per-user data-isolation guarantee, not just the storage mechanism beneath it):**
  - ✅ The PIN-wraps-the-token design (rather than PIN-derives-the-database-key) was a deliberate, documented choice made to close a real gap between the architecture doc's literal formula and its own stated security prose — flagged and reasoned through here and in `LocalUnlockService`'s doc comment, not silently picked.
  - ✅ Cross-profile isolation demonstrated by actual attempted cross-access (profile 1's PIN against profile 2's data), not just by two profiles independently working — this is exactly what the original task's Verify bar asked for and what a code-only review couldn't have confirmed.
  - ✅ No new private-key exposure: `LocalUnlockService` only ever handles the already-public enrollment token and symmetric key material already covered by prior review (`K_device`, `K_user`) — no new key type introduced.
  - **Verdict: safe. Task 6 complete** — mechanism, PIN gate, and real two-profile isolation all independently proven on the real deployment target.
- **Review Agent (self-run — this task is squarely "any code that touches flutter_secure_storage... or local database encryption"):**
  - ✅ PIN never stored, hashed, or logged anywhere — exists only transiently as Argon2id input; correctness proven by cipher success/failure, not a comparable stored artifact.
  - ✅ `K_device` kept separate from the Trust Anchor 2 signing key rather than reusing it as symmetric key material — a deliberate choice against a known key-reuse smell, not an oversight.
  - ✅ **Update (2026-09-10): the central finding of this task has flipped, and was re-verified rather than assumed.** Encryption-at-rest is now confirmed active on both Windows and real Android hardware, with real ciphertext checks (not "the code runs without error") on both — raw file bytes inspected directly, wrong-key rejection confirmed, right-key success confirmed, on-device via the same rigor as Task 5's airplane-mode proof.
  - ✅ No attempt was made to paper over the earlier finding while it stood, and no UI was built on top of the mechanism until it was independently proven — the PIN pad UI is still deliberately not built, now because it hasn't been gotten to yet rather than because the layer beneath it was unproven.
  - **Verdict: safe to proceed to building the PIN pad UI and multi-profile demonstration on top of this mechanism** — the storage layer it would rely on is now genuinely proven, on the real deployment target, not assumed.
- **Checkpoint saved:** 2026-09-10

## Task 7: Roster UI and multi-user switching
- **Status:** COMPLETE — real roster screen, two profiles, tap-to-switch fully verified offline on real Android hardware 2026-09-10 (autonomous continuation)
- **Setup:** Task 6 complete.
- **Work:** `RosterVaultApp.home` is now `RosterScreen` (`lib/screens/roster_screen.dart`) — the app's actual entry point, not `AuthGate` (renamed from private `_AuthGate`, now the "Add a person" sub-flow pushed as a route). New `lib/services/roster_registry_service.dart`: a device-wide list of `{userId, displayName}`, written whenever a PIN is set up *or* successfully unlocked (idempotent, self-healing — see the setState bug below for why "unlocked too" turned out to matter). Display name is the person's email, fetched via `Amplify.Auth.fetchUserAttributes()` at the moment it's captured — Cognito's own `username` for this pool is the opaque `sub`, not anything a roster should show a real person (see the service's own class doc).

  Tapping a roster entry shows a real PIN dialog (`_PinPromptDialog`), then calls `LocalUnlockService.unlock` — correct PIN pushes `ProfileHomeScreen`, which runs the actual `OfflineVerifier.verify()` against the recovered token (the same check Task 5 proved correct) and shows PASS/FAIL on screen; wrong PIN shows a SnackBar and stays on the roster. `ProfileHomeScreen`'s "Sign Out" is deliberately just `Navigator.pop()` — Task 7's own "fast switch... no round trip" bar is satisfied by there being nothing else to do, since nothing was ever held open beyond that screen's local variables. "Add a person" pushes `AuthGate` as a route (reusing Task 4b/4c/6's already-proven sign-in/enroll/PIN flow unchanged) and refreshes the roster on return; a new "Done" button on `DeviceDebugScreen`'s AppBar (shown only when pushed, via `Navigator.canPop()`) is how a real person gets back to the roster without accidentally signing out of the profile they just set up. `connectivity_plus` added to grey out "Add a person" when offline — per Multi-User Partitioning.md's own words, "simply unavailable — greyed out, honestly" — the one online-only surface in an otherwise fully local screen.

- **A real bug, caught live on the device, not in review:** `_refreshRoster()`'s first version was `setState(() => _rosterFuture = _rosterRegistryService.listProfiles())` — an arrow body, whose value is the assignment expression's own value (the `Future` itself). Flutter's `setState` explicitly rejects a callback that returns a `Future` ("setState() callback argument returned a Future") and throws before the rebuild completes. The roster registry *write* succeeded every time; the *screen* just never re-read it, silently, until a fresh app launch happened to pick up the persisted data on its own. Confirmed via `adb logcat` (the exact exception), fixed by using a block body instead — see `_refreshRoster`'s own comment for why this exact shape is a trap worth flagging for next time.
- **Verify — every step actually observed live on the real Samsung Galaxy A06, not inferred:**
  1. Fresh roster: correctly empty ("No one is enrolled on this device yet"), "Add a person" enabled (online).
  2. Tapped "Add a person" → real Cognito session already active (from Task 6) → real re-enrollment (**Signature: VALID**, the earlier `flutter_secure_storage` migration had wiped the previous token) → unlocked with the already-set PIN (`5678`) → **`PASS`** → registered on the roster → tapped "Done" → back at the roster, entry now showing `test-task6b@rostervault.dev` (a real name, not a UUID).
  3. Tapped that entry → real PIN dialog → correct PIN → landed on `ProfileHomeScreen`: **"Welcome, test-task6b@rostervault.dev" / "Offline sign-in: PASS"** — a real `OfflineVerifier.verify()` result, on-screen, triggered purely by picking a name and entering a PIN.
  4. "Sign Out" → instant return to roster (no spinner, no network call — the actual fast-switch guarantee).
  5. Added the second profile the same way (signed out of the pushed `AuthGate`'s Cognito session, signed in as `test-task4a`, re-enrolled, unlocked with `1234`) — roster now shows **both** real names.
  6. **Enabled airplane mode** (`adb shell cmd connectivity airplane-mode enable`, confirmed via `settings get global airplane_mode_on` = `1` and the status-bar icon) — confirmed "Add a person" correctly greys out to "Add a person (offline)". Tapped `test-task4a` → correct PIN → **"Welcome, test-task4a@rostervault.dev" / PASS**, airplane icon visible on screen. Signed out, tapped `test-task6b` → correct PIN → **"Welcome, test-task6b@rostervault.dev" / PASS**, still airplane mode on. This is the literal Verify bar: *"A user can tap between them repeatedly, offline, each time landing in the correct person's data."* Restored connectivity afterward.
- **Save:** Full multi-user offline flow demonstrated end to end — real roster, two real independently-enrolled profiles, correct-PIN unlock landing each person in their own verified session, wrong-profile isolation (inherited unchanged from Task 6's already-proven `LocalUnlockService`), fast switch with no network call, and the whole thing proven with the device's network genuinely disabled. Nothing outstanding for this task.
- **Depends on:** Task 6
- **Files changed:** `lib/screens/roster_screen.dart` (new — `RosterScreen`, `ProfileHomeScreen`, `_PinPromptDialog`), `lib/services/roster_registry_service.dart` (new), `lib/main.dart` (`_AuthGate` → public `AuthGate`, `RosterVaultApp.home` → `RosterScreen`, `_registerOnRoster` helper called from both the PIN-setup and unlock success paths, "Done" AppBar action on `DeviceDebugScreen`), `pubspec.yaml`/`pubspec.lock` (`connectivity_plus` added).
- **Review Agent (self-run — this task wires together every trust anchor built so far into the actual user-facing flow):**
  - ✅ No new key material or private-key exposure — `RosterRegistryService` stores only `userId` + a display email, never a token or key; `ProfileHomeScreen` reuses `OfflineVerifier`/`UserIdentityService` exactly as already reviewed in Tasks 5/6, no new crypto surface.
  - ✅ Cross-profile isolation is inherited, not reimplemented — `RosterScreen`/`ProfileHomeScreen` call the same `LocalUnlockService.unlock` already proven to reject a wrong profile's PIN in Task 6; no parallel unlock path was invented that could diverge from that guarantee.
  - ✅ The one online-only surface ("Add a person") is honestly gated on real connectivity state, not just documented as a policy — `connectivity_plus`'s live stream, not a static check-once flag, so it degrades correctly if connectivity drops mid-session.
  - **Verdict:** safe. Task 7 complete — the actual user-facing "pick your name" experience this whole project exists to deliver, proven live, offline, with two real people.
- **Checkpoint saved:** 2026-09-10

## Task 8: Epoch, revocation, and silent refresh
- **Status:** COMPLETE — server-side revocation proven via CLI, both the positive (refresh succeeds) and negative (refresh refused for a revoked user) client-side cases proven on real hardware.
- **Setup:** Task 7 complete.
- **Work:** `amplify/functions/enroll/handler.ts` now reads the existing `DeviceEnrollments` row *before* writing: if `status === 'revoked'`, throws (`"enroll: this enrollment has been revoked"`) instead of falling through to the unconditional `PutItemCommand`. This ordering is load-bearing — checking after the write would silently un-revoke the row on every subsequent refresh attempt, making revocation unenforceable online at all (documented inline in the handler). Epoch is preserved across re-enrollment as before (Task 4a); revocation is a separate `status` flag rather than epoch alone, because nothing in a stateless Lambda invocation lets it compare "device's last known epoch" against "current epoch" the way `OfflineVerifier` does on-device — the online gate needed its own explicit, server-checkable flag.

  Client side: `ProfileHomeScreen._refresh()` (`lib/screens/roster_screen.dart`) explicitly checks `Amplify.Auth.getCurrentUser().userId == widget.entry.userId` before attempting anything — a direct consequence of Amplify Auth's single-active-Cognito-session-per-device model (documented in Task 7 already, applies again here): a background refresh can only ever act for whichever profile's session happens to be currently active, not silently for every enrolled profile at once. If it matches, calls `EnrollmentService.enroll()` again (the same mutation Task 4a/7 already proved) and reports "Refreshed — access confirmed still active." on success, or "Refresh refused: ..." on the GraphQL error surfacing the Lambda's revocation message. If the active session belongs to someone else, shows a clear explanatory message rather than a confusing failure. Found and fixed a real bug while wiring this in: `Amplify.configure()` had only ever been called lazily inside `AuthGate._bootstrap()`, but Task 7 made `RosterScreen` (not `AuthGate`) the app's home, and its normal offline-unlock path never touches `AuthGate` — so a fresh process that goes straight from roster → PIN → profile → Refresh would throw `PluginError: "Auth plugin has not been added to Amplify"`. Fixed by extracting `ensureAmplifyConfigured()` to a top-level function called unconditionally from `main()`, defensively also called at the top of `_refresh()`.

- **Verify — what was actually observed, split honestly by what is and isn't confirmed:**
  1. **Server-side revocation logic — fully verified via CLI, real AWS, not mocked.** Obtained real Cognito ID tokens for both test users via `admin-initiate-auth`, called the deployed AppSync `enroll` mutation directly with `curl`: baseline refresh for `test-task6b@rostervault.dev` succeeds. Revoked via `aws dynamodb update-item` (bumped `epoch` 0→1, set `status: revoked`) on that user's `DeviceEnrollments` row. Repeated the same mutation call: **refused**, with the exact Lambda error `"enroll: this enrollment has been revoked"` surfaced through AppSync. Called the *other* test user's (`test-task4a@rostervault.dev`) refresh in between: **unaffected**, succeeded normally. This is the core "revocation actually works, server-side" proof this task exists to deliver, and it's solid.
  2. **Client-side positive case — verified on the real Samsung Galaxy A06.** Unlocked `test-task4a` (PIN `1234`, not yet revoked), tapped "Refresh now" on `ProfileHomeScreen`: **"Refreshed — access confirmed still active."** on screen, screenshotted.
  3. **Client-side refused case — verified on the real device, resumed after a device re-authorization blocker.** To show this, `test-task6b` (revoked in step 1) needed to become the active Amplify session. Mid-sequence, the device's USB adb connection dropped to `unauthorized` (Android's own "Allow USB debugging from this computer?" re-prompt, requiring a tap on the device's own screen) — logged honestly at the time as a genuine hardware/attendance blocker rather than retried in a loop. On resuming, the device had re-authorized on its own; continued the exact sequence: real Amplify sign-out from `DeviceDebugScreen`, signed in as `test-task6b`, "Done" back to roster (no re-enrollment), tapped `test-task6b`'s roster entry, entered its real PIN, landed on `ProfileHomeScreen` with **"Offline sign-in: PASS"**, tapped "Refresh now": **"Refresh refused: Bad state: enroll mutation failed: [...message: enroll: this enrollment has been revoked}]"** — the real GraphQL error from the real deployed Lambda, rendered on screen exactly as `_refresh()`'s catch branch was built to show it.
- **Save:** Revocation-by-epoch demonstrated end to end, both server-side (CLI: refuse the revoked user, other user unaffected) and client-side on real hardware (both the happy-path refresh and the refused-for-revoked-user outcome, each with the real profile whose DynamoDB row was actually mutated). Nothing outstanding for this task.
- **Depends on:** Task 7
- **Files changed:** `amplify/functions/enroll/handler.ts` (revocation check), `lib/screens/roster_screen.dart` (`ProfileHomeScreen._refresh()` + UI section), `lib/main.dart` (`ensureAmplifyConfigured()` extracted to top-level, called from `main()`).
- **Review Agent (self-run):**
  - ✅ Revocation check ordered before the write, not after — verified this is the correct order by reasoning through what an after-write check would do (silently un-revoke every refresh), and confirmed the deployed behavior matches via the CLI test in step 1 and the on-device result in step 3.
  - ✅ No new key material or crypto surface — refresh reuses the exact `EnrollmentService.enroll()` call already reviewed in Task 4a/7; nothing new to review there.
  - ✅ The single-active-session scoping-down (only the currently-active profile's session can be refreshed, not all enrolled profiles silently in the background) is flagged in the code's own doc comment and here, not silently narrowed from the task's original "surface refresh outcome per profile" phrasing.
  - **Verdict:** safe. Task 8 complete — revocation is something you watch happen on screen, both ways, not something inferred from logs.
- **Checkpoint saved:** 2026-09-10

## Task 9: Hardening
- **Status:** NOT STARTED
- **Setup:** Task 8 complete.
- **Work:** Clock-rollback defence (persisted high-water-mark timestamp check). Best-effort root/jailbreak detection with graceful degradation, not a hard block. Lockout after repeated failed local-factor attempts, tracked per profile.
- **Verify:** **Seventh on-screen milestone:** setting the device clock backward produces a visible, specific on-screen message and blocks sign-in — not a silent failure. Repeated wrong PINs show a visible lockout state for that profile without affecting other enrolled profiles on the same device.
- **Save:** [[../Security/Risk Register|Risk Register]] mitigations for rows 1, 2, and 3 all demonstrated, not just designed.
- **Depends on:** Task 8

## Task 10: End-to-end testing and traceability review
- **Status:** NOT STARTED
- **Setup:** All previous tasks complete.
- **Work:** Full test suite run. [[../../agents/traceability-agent/traceability-agent|Traceability Agent]] pass against [[../Management/The Actual Requirement|The Actual Requirement]] and the acceptance criteria implied by each task above.
- **Verify:** All tests pass. Traceability matrix shows no gaps, or gaps are explicitly logged as known follow-ups.
- **Save:** Story marked DONE.
- **Depends on:** Task 9

---

## Plan Changes Log

- **2026-09-09:** Initial plan drafted from the architecture note, decomposed into 9 tasks (0 documentation + 8 build tasks) per Story Agent scoring (12, Large).
- **2026-09-09:** Scope change — inserted a new Task 1 (GitHub repository + Amplify Hosting git connection) ahead of the Amplify Gen 2 skeleton. Amplify's hosted backend build pipeline requires a connected git provider; the repo had no remote and no commits yet. All subsequent tasks renumbered (old Task 1 → 2, ... old Task 9 → 10). Flagged by Nigel mid-Task-1-execution, before any AWS deploy happened.
- **2026-09-09:** Task 4 sub-decomposed into 4a (Enrollment Lambda + AppSync mutation, backend-only), 4b (Flutter Cognito login screen), 4c (wire end to end + local storage of the token) — too big for one sitting as a single task per the Story Agent's own complexity rubric. Confirmed with Nigel: Task 4c uses placeholder (`flutter_secure_storage`-only) local storage, not pulling Task 6's PIN/Argon2id wrapping forward. Not yet approved to start execution.
- **2026-09-09 (autonomous mode begins):** Nigel explicitly authorized continuing through Task 4a onward (4b, 4c, then 5–10) without pausing for live checkpoints — he's stepping away and will review this file in one sitting. Directive: pick recommended/lowest-risk defaults at Story Agent Phase-3 decision points instead of waiting, log every such decision here, keep this file rigorously accurate as the substitute for the live review. Guardrails that stayed in force throughout: stay confined to this repo's own Amplify backend; never touch the sibling `where_in_nz_v3` project or the pre-existing unrelated `roster-app` (appId `d2wnajnfhijfg7`) sharing this AWS account (re-confirmed via `aws amplify list-apps` before any AWS write this session — same three apps as Task 1's incident log, nothing added or removed); no `git push`, no PR, local commits only; no destructive/irreversible action beyond what's already agreed (sandbox `RemovalPolicy.DESTROY` posture, no force-push, no history rewriting). Partway through Task 4a, Nigel further relaxed two of the original stop conditions: (1) "no obvious recommended default" is no longer a hard stop — just make the call and log it (this and all entries below reflect that); (2) missing Android emulator/device is no longer a hard stop for the *whole* run — document the gap honestly, skip only the specific sub-step that needs it, keep going on everything else.
- **2026-09-09 (Task 4a, autonomous):** Confirmed via `aws sts get-caller-identity` + `aws amplify list-apps` before any write — same account (445567075330), same three apps as Task 1's incident log (`roster-vault` ours, `where_in_nz_v3` and `roster-app` untouched). `flutter emulators --create` attempted once as instructed; failed with no Android AVD system images available (a sizeable download, consistent with Task 3's existing note) — not pursued further per the relaxed device stop condition; Task 3's outstanding P-256 verification and any later device-dependent milestones remain genuinely blocked on Nigel providing hardware or approving the system-image download, not faked.
- **2026-09-09 (Task 4a, autonomous):** Circular nested-stack dependency between the enrollment function's default stack and the manually-created `RosterVaultCore` stack (CloudFormation rejected it outright — not a style choice, a hard deploy failure). Fixed by moving the function into `RosterVaultCore` via `resourceGroupName` and obtaining that stack via `backend.enroll.stack` instead of a second `createStack` call. Side effect: the `DeviceEnrollments` table and the KMS key's *alias* (not the underlying CMK — same key id `fdf9ab21-...` throughout, confirmed in `amplify_outputs.json` after redeploy) were recreated as new physical resources during the stack restructuring. Acceptable under the already-agreed sandbox `RemovalPolicy.DESTROY` posture (no live enrolled devices exist yet); `docs/roster-vault/Backend/Backend Index.md` updated to stop hardcoding the table name, since it now changes with stack topology.
- **2026-09-09 (Task 4a, autonomous, Review Agent self-check):** Two items found and fixed before commit, not just noted: (1) `grantReadWriteData()` was broader than the handler's actual `GetItem`+`PutItem` usage — tightened to exactly those two actions; (2) the issuer public key had been fetched for CLI verification but not actually baked into the app as Task 4a's own scope required — added `lib/services/issuer_public_key.dart` before treating the task as done. Full detail under Task 4a above.
- **2026-09-09 (Task 4a, autonomous):** Added `ALLOW_ADMIN_USER_PASSWORD_AUTH` to the Cognito app client's explicit auth flows so this project's own AWS-credentialed tooling could obtain a real ID token for CLI verification without hand-implementing SRP. Deliberately not the broader `ALLOW_USER_PASSWORD_AUTH` (would expose direct password auth to any client holding the app client id) — `admin-initiate-auth` requires the caller to already hold AWS IAM credentials, a narrower and more defensible surface. The one end-user-facing flow, SRP, is unchanged. Judgment call under the relaxed decision-point rule; reasoning recorded here and in Backend Index.
- **2026-09-09 (Task 4a, autonomous):** Simplified the enrollment response to `{token, epoch, expiresAt}` rather than the architecture diagram's "token + device certificate" — no concrete shape for a "device certificate" exists anywhere in the docs, and inventing one without further spec would be guessing at a security-relevant design detail rather than a low-stakes default. Flagged here rather than silently dropped; worth Nigel's explicit input on whether a device certificate is actually needed and what it should contain, before Task 8 (revocation) potentially needs it.
- **2026-09-09 (Task 4a, autonomous):** Found and fixed a pre-existing, unrelated test failure (`flutter test` — pending Timer from Task 3's 15s device-keygen timeout, never advanced past in the widget test). Not part of Task 4a's own diff, but left broken it would have violated "every task ends in a stable state" for whatever task ran next. See the dedicated note under Task 4a above; this also means Task 3's checkpoint's "flutter test... passing" claim was inaccurate at the time and should be read as corrected here.
- **2026-09-09 (Task 4b, autonomous):** The checkpoint's verify bar for Task 4b ("a real person taps through") isn't literally achievable with no human present. Chose real Win32 mouse/keyboard input simulation against the actual compiled, running window over a `flutter test` widget-pump alternative, specifically because `Amplify.configure()`/`Amplify.Auth.signIn()` can touch real platform-channel plugins (secure storage) that don't exist in a plain widget test's sandbox — a widget test could have given a misleading pass or fail unrelated to whether sign-in genuinely works. The Win32 approach drives the exact same binary a real user would run. Result: real SRP sign-in against the deployed Cognito pool succeeded, screenshotted.
- **2026-09-10 (Task 4c, autonomous):** `flutter test` hung indefinitely (confirmed via `TaskStop`, no error, no timeout) when a test tried to call `Amplify.Auth.signIn()` outside a real running app — a second, independent instance of the same class of problem as the Windows Hello hang below (native/platform-channel-backed plugin behavior differing between `flutter test` and `flutter run`). Killed the hung test and removed it rather than leave an unreliable test in the suite; `EnrollmentService`'s own logic is instead verified by inspection-plus-precedent (identical mutation shape to Task 4a's already-proven CLI call) rather than a second live network test.
- **2026-09-10 (Task 4c, autonomous):** Diagnosed a genuine hang in the live UI click-through (see Task 4c's Verify section for full detail): `biometric_signature`'s user-key creation waits on a Windows Hello consent prompt that never resolves, because no Windows Hello PIN is configured on this machine. Root-caused (not assumed) by enumerating all system windows during the hang and finding three orphaned `CredentialUIBroker.exe` processes, invisible in any app-window screenshot. Cleaned up the orphaned processes. Per Nigel's relaxed device/hardware stop condition: documented honestly, did not fake the missing on-screen proof, and verified everything else (mutation call, signature verification) that doesn't depend on this specific native call.
- **2026-09-10 (Task 4c, autonomous):** `package:cryptography`'s `Ecdsa.p256` turned out to be a hard dead end on this platform — its entire pure-Dart P-256 implementation unconditionally throws `UnimplementedError` (verified by reading `cryptography-2.9.0/lib/src/dart/ecdsa.dart` directly, not just inferring from the error). Rather than pursue a workaround, switched to `dart_jsonwebtoken` — which docs/roster-vault/Backend/Tech Stack Mapping.md had specified for this exact job from the start. The `cryptography` package remains a dependency for Task 6's planned Argon2id use, with a comment in `token_verifier.dart` warning future readers about the ECDSA limitation so it isn't rediscovered the hard way twice.
- **2026-09-10 (Task 4c, autonomous, judgment call under the relaxed decision-point rule):** Committed a real KMS-signed test token as a fixture in `test/token_verifier_test.dart` rather than only a synthetic one. Reasoning logged in Task 4c's Review Agent section above — flagged explicitly rather than treated as an obviously-safe default, given this project's stated bar for treating tokens like credentials.
- **2026-09-10 (Task 5, autonomous):** Re-read docs/roster-vault/Security/Token Lifecycle and Revocation.md before implementing the epoch check rather than guess at security-critical logic — confirmed it must compare against a separately-persisted on-device value from the last successful enrollment/refresh, not the token's own epoch claim. Extended `EnrollmentService` accordingly (a small, necessary completion of Task 4c's own scope, not new scope).
- **2026-09-10 (Task 5, autonomous):** Deliberately did not attempt to disable this machine's real network connectivity to demonstrate "airplane mode" — the shell tools this session runs through execute on the same machine being tested, so cutting its network access risks severing this session's own ability to continue at all, with no way to reverse it once done. Considered and rejected a safer alternative (a Windows Firewall rule scoped to just the app's own process) as moot given no real enrolled token exists yet to test with regardless. Logged as a genuine operational-risk judgment call, not a hardware gap covered by the relaxed stop condition.
- **2026-09-10 (Task 5, autonomous):** Spent real effort diagnosing a Win32 UI-automation anomaly (clicking "Sign in offline" instead triggered "Enroll this device") rather than either faking a clean screenshot or silently giving up. Ruled out DPI scaling (checked: 96 DPI, 100%) and a stationary-cursor-over-new-widget theory (added cursor-parking, didn't fix it). Stopped investigating further once it was clear this is a test-harness question rather than a production-code defect — the same button-wiring pattern already works correctly for the Enroll button — and reported the gap honestly rather than continuing to spend turns on it.
- **2026-09-10 (Task 5, autonomous):** Left the production challenge-response signer (`_productionChallengeSigner` in `lib/main.dart`) as an explicit `UnimplementedError` rather than guessing at `biometric_signature`'s raw-signature byte format (DER vs. IEEE P1363), which isn't documented and can't be verified without real hardware. Guessing wrong would have shipped a silently-broken challenge-response that always fails on a real device — worse than a clearly-flagged gap.
- **2026-09-10 (Task 6, autonomous):** Before building on any `package:cryptography` algorithm this time, smoke-tested Argon2id, HKDF, and AES-GCM individually with `dart run` first — a direct lesson from Task 4c/5's Ecdsa dead end. All three are genuinely implemented (unlike Ecdsa). Worth the extra step; it caught nothing this time, but would have caught it early if it had.
- **2026-09-10 (Task 6, autonomous):** `sqlcipher_flutter_libs` added, found to break the Windows build (missing OpenSSL), and reverted within the same work session rather than left broken or worked around by installing system-wide dev tools (outside the authorized "this repo and its own Amplify backend only" scope). Verified the revert actually restored a working build via a real `flutter build windows` before continuing. This is the most significant finding of the autonomous run so far — logged in detail in Task 6's own entry above rather than just here.
- **2026-09-10 (Task 6, autonomous):** Chose not to build the PIN entry UI / fourth on-screen milestone this round, once it was clear the storage layer it would visibly claim to protect isn't actually encrypted here. Building UI on top of a just-discovered-broken security mechanism, even with an honest disclaimer buried in a doc comment, risks the UI itself being read as "this works" by anyone skimming the app rather than the checkpoint. Judgment call under the relaxed decision-point rule: mechanism-and-tests over UI-that-would-oversell-it.
- **2026-09-10 (Task 6, autonomous):** Defined `K_device` as a new, dedicated randomly-generated symmetric secret (`DeviceMasterKeyService`) rather than reusing the Trust Anchor 2 ECDSA device key as HKDF input material — the architecture docs specify `K_user = HKDF(K_device, user_id)` precisely but not what `K_device` concretely is. Flagged rather than silently assumed, since getting this wrong would be a real crypto-design mistake (key reuse across purposes), not a cosmetic one.
- **2026-09-10 (live session with Nigel, real Android hardware):** Autonomous execution paused; Nigel returned and connected a real Samsung Galaxy A06 (Android 16 / API 36) to actually verify Task 3's outstanding ECDSA P-256 item. Significant adb/device connectivity friction along the way (wireless pairing failed with an unresolved `protocol fault`, worked around via USB; a genuine touchscreen-unresponsiveness incident on the phone itself, resolved by restarting it) — see [[../Troubleshooting/Known Issues|Known Issues]] for full detail. Real sign-in and navigation to `DeviceDebugScreen` succeeded, but device-key generation hit a new, different blocker: `biometric_signature` 13.0.0's `createKeys()` fails with `BiometricError.unknown "Foreground activity required"`, reproducibly, root-caused by reading the plugin's own Kotlin source rather than guessed at. Applied a temporary fix — `pubspec.yaml`'s `biometric_signature` pinned to an unreleased git commit past 13.0.0 that specifically targets the AGP9/KGP class of problem Flutter's own build output had warned about for this plugin. **Not yet verified working** as of this entry — testing it was blocked by Claude Code's own permission classifier denying a `flutter run` launch. See Task 3's entry above and [[../Troubleshooting/Known Issues|Known Issues]] for full detail; do not treat the fix as confirmed until a later checkpoint entry says so.
- **2026-09-10 (continued, live session with Nigel):** Nigel approved the previously-blocked `flutter run` launch and ran it directly. Rebuilt with the git-pinned commit (32.1s, a genuine recompile) and reinstalled on the same device: **identical failure.** The AGP9/KGP-Gradle-DSL fix does not address the runtime null-`activity` bug — confirmed a dead end, not merely still-pending. Reverted `pubspec.yaml` to the hosted `^13.0.0`. Searched the plugin's GitHub issues for the exact error text and more broadly for "activity" — no match exists upstream. Updated `Known Issues.md`, `Trust Anchors.md`, and Task 3's entry above to state this as a confirmed negative result rather than "unverified," so nobody reads the earlier entries as still-pending. Real Android P-256 verification remains blocked; untried next steps are bisecting older plugin versions, trying an alternative plugin, or filing the existing root-cause diagnosis upstream.
- **2026-09-10 (resolved, live session with Nigel):** Nigel asked for the "best solution" for the Android blocker and specifically invited disagreement with his own suggestion (switching plugins). Before doing either bisect-old-versions or migrate-to-a-different-plugin, reread the `biometric_signature` source that had already been quoted in Known Issues — `activity` is typed `FlutterFragmentActivity?`, populated via `binding.activity as? FlutterFragmentActivity`. This project's `MainActivity.kt` extended plain `FlutterActivity`. Checked the plugin's `README.md` directly (not just its source) and found the actual documented requirement: *"This plugin requires the use of a `FragmentActivity` instead of `Activity`. Update your `MainActivity.kt` to extend `FlutterFragmentActivity`."* This was never a plugin bug — it was a missing required setup step from Task 3's original scaffolding. Changed `MainActivity.kt` accordingly, rebuilt, reinstalled on the same real device: **real ECDSA P-256 key generated** (Device ID `fe8888db42d5c63fddb42e91291a0481`, EC/P-256-shaped SPKI public key). Immediately also tapped through real enrollment on the same device — **Signature: VALID**, full decoded claims on screen — and real offline sign-in against the real stored token, which correctly reached and typed-FAILed at the one deliberately-unimplemented step (`_productionChallengeSigner`). Found and fixed one more small real bug along the way: `DeviceDebugScreen`'s body overflowed off-screen on this device's resolution (no scroll container) — wrapped in `SingleChildScrollView`. Updated Task 3 to COMPLETE, Task 4c to COMPLETE, Task 5's entry with the new real-hardware evidence (still not COMPLETE — no real PASS yet), and rewrote (not just appended to) the relevant `Known Issues.md` and `Trust Anchors.md` sections that had mis-described this as an unresolved third-party plugin bug, since that framing was actively wrong, not just incomplete.
- **2026-09-10 (Task 5 completed, live session with Nigel):** Nigel said "carry on" after reviewing the above. Resolved the production challenge signer's last open question by reading `biometric_signature`'s Android source directly rather than guessing: it signs via plain `Signature.getInstance("SHA256withECDSA")`, standard JCA, always DER — no P1363 path exists in the plugin at all. Implemented `UserIdentityService.signChallenge()`: builds the ES256 JWS signing input, signs via `createSignature(signatureFormat: SignatureFormat.raw)`, converts the DER result to raw 64-byte `r‖s` via a direct Dart port of the Lambda's already-proven `derToRawEcdsaSignature`. Rebuilt, reran "Sign in offline" on the real device: **`Result: PASS`.** Then, since real hardware was in hand, went further than the checkpoint's own stated bar required proving right now: enabled airplane mode on the phone via `adb shell cmd connectivity airplane-mode enable` (a separate device reached only over USB — doesn't touch this dev machine's own network, so the earlier-logged risk about cutting *this PC's* connectivity doesn't apply), confirmed via `settings get global airplane_mode_on` = `1` and the status-bar icon, tapped "Sign in offline" again: **`Result: PASS` with the device's network genuinely disabled.** Restored normal connectivity afterward. Task 5 marked COMPLETE — nothing outstanding.
- **2026-09-10 (Task 6 storage resolved, live session with Nigel):** Continued from Task 5's "carry on." `sqlcipher_flutter_libs`, the obvious next attempt for Task 6's real-encryption gap, turned out to be EOL upstream (`0.7.0+eol`, "Not used anymore, update to version 3.x of package:sqlite3 instead" — checked pub.dev directly rather than assuming it still applied). Followed that pointer to `package:sqlite3` 3.x's native cipher support via Dart's build hooks; read `sqlite3.dart`'s own `doc/hook.md` and specifically avoided the `sqlcipher` source variant once its docs revealed it still needs OpenSSL on Windows/Linux/Android — the exact blocker being escaped — choosing `sqlite3mc` instead. Upgrading `drift`/`sqlite3` to versions compatible with each other cascaded into a real pub dependency conflict against `amplify_auth_cognito`'s transitive `sqlite3` pin, resolved by bumping `flutter_secure_storage` and the `amplify_*` packages together. That surfaced two more independent, real Android build failures (`compileSdk 37` requirement, a stale generated plugin registrant) — both root-caused and fixed properly, not worked around. Verified real encryption with a raw-bytes check on Windows first, then — same discipline Task 5 established — on the real Android device via a new on-screen debug test: **PASS** on both. Full detail, including a self-healing `flutter_secure_storage` migration quirk hit along the way, in [[../Troubleshooting/Known Issues|Known Issues]]. Task 6 still not COMPLETE — the PIN pad UI and multi-profile demonstration are the only things left in it now.

## Doc Update — 2026-09-10 (Docs Agent, live session)

### Files Updated
- `Security/Trust Anchors.md` — added an Android-specific caveat under Trust Anchor 2 alongside the existing Windows RSA-fallback caveat: `biometric_signature` 13.0.0 fails on real Android 16 hardware too, for a different reason, and the P-256 guarantee remains unverified on real hardware as a result.
- `Troubleshooting/Troubleshooting Index.md` — populated with a link to the new Known Issues page (was previously a placeholder).
- `stories/story-checkpoint-mvp.md` — Task 3's entry updated to reflect that real Android hardware was reached this session but hit a new blocker before device-key generation could succeed; the old "just needs an Android or iOS run" framing is now stale and superseded, not still the whole story.

### Files Created
- `Troubleshooting/Known Issues.md` — five entries: the `biometric_signature` 13.0.0 Android bug (full root-cause + temporary fix, explicitly marked unverified), the unresolved wireless-adb-pairing `protocol fault`, the phone's mid-session touchscreen-unresponsiveness incident, and two `adb shell input` automation gotchas (keyboard-shifts-layout, cursor-not-at-end-after-tap) hit while scripting a real sign-in form.

### Notes
- Flagged, not fixed (out of scope for this pass): the wireless-adb-pairing `protocol fault` was worked around (switched to USB) rather than root-caused — the adb-key regeneration attempted as a fix was never actually confirmed to work, since the team didn't retry wireless pairing after applying it. Worth a follow-up if wireless debugging is needed again on this machine.
- ~~Flagged, not fixed: the `biometric_signature` git-pin fix itself still needs on-device verification...~~ **Resolved below** (2026-09-10, same day) — the git-pin fix was tested and confirmed not to work, and the real root cause was found and fixed instead.

## Doc Update — 2026-09-10 (Docs Agent, live session, correction pass)

Follow-up to the Doc Update above. That pass documented the `biometric_signature` bug as an unresolved, unattributed third-party plugin issue. That framing turned out to be wrong — this update corrects the substance of what was written, not just appends to it, per the Docs Agent's "flag drift, don't just fix it" directive applied to the agent's own prior drift.

### Files Updated (substance corrected, not just appended)
- `Troubleshooting/Known Issues.md` — the `biometric_signature` entry rewritten: retitled from an open bug to **RESOLVED**, the git-pin diagnostic detour kept (as a documented dead end, not deleted), and the actual root cause added — `MainActivity.kt` was missing `biometric_signature`'s own documented requirement to extend `FlutterFragmentActivity` instead of `FlutterActivity` (verbatim in the plugin's `README.md`, line 265). Fix, and the real ECDSA P-256 result it produced, now documented with the actual Device ID and public-key prefix as evidence.
- `Security/Trust Anchors.md` — Android caveat under Trust Anchor 2 rewritten from "still unverified on real hardware" to verified, with the `FlutterFragmentActivity` requirement kept as a setup note (not an open blocker).
- `stories/story-checkpoint-mvp.md` — Task 3 moved IN PROGRESS → COMPLETE; Task 4c moved MOSTLY COMPLETE → COMPLETE (full on-screen milestone achieved: real sign-in, real enroll, Signature: VALID with decoded claims); Task 5 updated with new real-hardware evidence (still MOSTLY COMPLETE, not COMPLETE — production challenge signer remains unimplemented, so only a correctly-typed FAIL was shown, not a PASS); top-level Status line and Plan Changes Log updated to match. Also noted the `SingleChildScrollView` fix for a real overflow bug hit live on the Android device.

### Notes
- The `SingleChildScrollView` fix (`lib/main.dart`) and the `MainActivity.kt` fix are both real shipped code changes from this session, alongside the doc corrections — see the Plan Changes Log entry immediately above this Doc Update for the full narrative.
- Still open, unrelated to this correction: the wireless-adb `protocol fault` root cause (flagged above, unchanged) and Task 5's remaining gaps (production challenge signer, airplane-mode demonstration).
- **2026-09-10 (Task 5 and Task 6 both completed, live session with Nigel):** Implemented the production challenge signer (`UserIdentityService.signChallenge`) by reading `biometric_signature`'s Android source directly rather than guessing — confirmed DER output, ported the Lambda's already-proven DER-to-raw conversion. Real PASS achieved on real hardware, then went further and proved the airplane-mode requirement literally: toggled the phone's airplane mode via `adb shell cmd connectivity airplane-mode` (safe — a separate device over USB, not this dev machine's own network, so the earlier-logged risk about cutting *this PC's* connectivity didn't apply), confirmed on and off via `settings get global airplane_mode_on`, got a real PASS with the device's network genuinely disabled. Task 5 marked COMPLETE. Immediately continued into Task 6's remaining PIN pad UI: found a real, worth-flagging tension between the architecture doc's literal `K_user = HKDF(K_device, user_id)` formula (no PIN in it) and its own prose claiming a local factor is required — resolved by having the PIN wrap the enrollment token itself rather than the database key, giving it genuine cryptographic weight instead of being a redundant UI-only gate (see Task 6's own entry for the full reasoning). Created a second real Cognito test user, enrolled two independent profiles on the same physical device, set two different PINs, and proved cross-profile isolation by literally trying one profile's PIN against the other's data and watching it get rejected — not inferred, observed on screen at every step. Task 6 marked COMPLETE. Nigel then went to sleep, having asked for the work to continue autonomously in the meantime (same authorization as the earlier autonomous-mode session) — see the final Plan Changes Log entry for how Tasks 7-10 were handed off.
- **2026-09-10 (Task 7 completed, autonomous continuation):** Built the real roster screen and made it the app's actual home, reusing every already-proven piece from Tasks 4-6 (`AuthGate`, `LocalUnlockService`, `OfflineVerifier`) rather than inventing a parallel path. Caught a real bug live via `adb logcat` — a `setState(() => future = ...)` arrow body silently returned the `Future` itself, which Flutter's framework rejects, so a real write (roster registration) was succeeding while the screen just never re-read it; fixed with a block body and documented the trap inline so it isn't rediscovered blind. Verified the actual "two profiles, tap between them, offline" milestone literally: enrolled two real people on the same device, then toggled airplane mode and switched between both, landing correctly in each one's own verified session both times. Flagged, not silently decided: the roster's `K_user`-vs-PIN cryptographic split (documented in Task 6) carries forward unchanged here since Task 7 only adds a UI layer on top, not a new storage/crypto path.
- **2026-09-10 (Task 8, autonomous continuation):** Deployed the revocation check to the real Lambda (`npx ampx sandbox --once`) and proved it via direct `curl` calls against the AppSync endpoint with real Cognito tokens (`admin-initiate-auth`) *before* touching the client — same "verify the hard part in isolation first" discipline as earlier tasks. Revoked `test-task6b`'s row via `aws dynamodb update-item` (epoch 0→1, status→revoked): refresh correctly refused with the Lambda's exact error string; `test-task4a`'s refresh in between was unaffected. Built the client `_refresh()` flow, hit and fixed two real bugs along the way (a stray orphaned `),` from an Edit, and an `Amplify.configure()` never being called because Task 7 made `RosterScreen` — not `AuthGate` — the app's home). Verified the happy-path refresh on the real device. While maneuvering the device to make the already-revoked `test-task6b` the active Amplify session (needed to screenshot the refused case), the USB adb connection dropped to `unauthorized` — Android's re-trust-this-computer prompt, which needs a tap on the device's own screen. Tried a server restart and a second wait; did not retry-loop further per the standing guardrail against looping on access blocks. Committed the mostly-complete state honestly rather than fabricating a screenshot.
- **2026-09-10 (Task 8 completed, resumed after device re-authorization):** On resuming, `adb devices` showed the device back to `device` state on its own (no intervention available or needed) — continued the exact sequence that had been blocked: real Amplify sign-out, signed in as `test-task6b` (fixing a text-input mis-target along the way — the password field's tap coordinate needed adjusting after the keyboard changed the layout, and a PIN-pad digit-order mistake on the first attempt correctly produced "Wrong PIN — rejected", itself a small confirmation the wrong-PIN path still works, then re-entered correctly), unlocked, tapped "Refresh now": **"Refresh refused: ...message: enroll: this enrollment has been revoked"** on screen, the real GraphQL error from the real Lambda. Task 8 marked COMPLETE.
- **2026-09-10 (Task 8, judgment call):** Left `test-task6b`'s `DeviceEnrollments` row as `status: revoked` / `epoch: 1` rather than restoring it to `active` afterward — it's the concrete evidence behind Task 8's Verify claim, and revocation only gates the *online* refresh path, not local PIN unlock, so this profile remains fully usable for Task 9's on-device work (lockout, clock-rollback) which only exercises the offline/local path.

---

## Notes

- No code has been written yet — this checkpoint exists so that Task 1 can start from an approved plan, per the Story Agent's "no code during planning" rule.
- Task 5 is the one to protect most carefully against being rushed or merged with another task — it's the actual hard requirement, and everything before it is enabling infrastructure.
- Revisit the TTL length (currently assumed 24–72h) once there's a real sense of how often these devices actually reconnect — it's a policy knob, not fixed by the architecture.
- **Flutter demo progression, at a glance** (added 2026-09-09 at Nigel's request — he wants to see the app actually take shape, not just infra land):
  1. Task 3 — app runs, shows device ID + device public key
  2. Task 4 — real OIDC login screen, enrollment completes on screen
  3. Task 5 — debug "sign in" button, pass/fail visible in airplane mode
  4. Task 6 — real PIN pad / biometric prompt replaces the debug button
  5. Task 7 — real roster screen, tap-to-switch between two people
  6. Task 8 — roster shows per-profile refresh/refuse outcome after a revocation
  7. Task 9 — visible on-screen block for clock rollback and for lockout
